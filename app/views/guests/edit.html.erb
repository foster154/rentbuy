<!-- about -->
<div class="container-fluid" style="padding:0;">

 	<% if @guest.email.present? && @guest.preapproval.blank? # supplied an email, but no preapproval %>
		<%= render 'guests/partials/thank_you' %>

	<% else %>
		<%= render 'guests/partials/property_results' %>
		
	<% end %>
					
</div>
<!-- /about -->